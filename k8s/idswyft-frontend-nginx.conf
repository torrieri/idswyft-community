# Pass through X-Forwarded-Proto from upstream proxy (e.g. Caddy), fall back to $scheme
map $http_x_forwarded_proto $forwarded_proto {
    default $http_x_forwarded_proto;
    ""      $scheme;
}

server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA index.html caching prevention to avoid stale security header caching
    location = /index.html {
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline' https://docs.opencv.org; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https://*.s3.amazonaws.com https://*.s3.us-east-1.amazonaws.com https://orkapi-idswyft.s3.us-east-1.amazonaws.com; connect-src 'self' data: blob:" always;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    }

    # LLM agent discovery — serve markdown docs when Accept header requests plain text
    location ~ ^/docs? {
        if ($http_accept ~* "text/plain|text/markdown") {
            rewrite ^ /llms-full.txt last;
        }
        try_files $uri /index.html;
    }

    # SPA fallback routes — serve index.html with caching prevention
    location / {
        try_files $uri $uri/ /index.html;
        
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline' https://docs.opencv.org; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https://*.s3.amazonaws.com https://*.s3.us-east-1.amazonaws.com https://orkapi-idswyft.s3.us-east-1.amazonaws.com; connect-src 'self' data: blob:" always;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    }

    # Proxy API requests to the backend
    location /api/ {
        proxy_pass http://idswyft-api:3001/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        proxy_cache_bypass $http_upgrade;

        # Strip Origin header — requests through this proxy are same-origin
        # by definition, so CORS checks are unnecessary. This avoids requiring
        # CORS_ORIGINS config for the standard self-hosted Docker setup.
        proxy_set_header Origin "";

        # File uploads can be large (ID documents)
        client_max_body_size 20M;
    }

    # Cache static assets safely
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline' https://docs.opencv.org; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https://*.s3.amazonaws.com https://*.s3.us-east-1.amazonaws.com https://orkapi-idswyft.s3.us-east-1.amazonaws.com; connect-src 'self' data: blob:" always;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    }

    # Fallback default security headers for other types of requests
    # add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline' https://docs.opencv.org; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https://*.s3.amazonaws.com https://*.s3.us-east-1.amazonaws.com https://orkapi-idswyft.s3.us-east-1.amazonaws.com; connect-src 'self' data: blob:" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
}
